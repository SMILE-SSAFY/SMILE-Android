<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">
    <ImageView
        android:id="@+id/iv_view"
        android:layout_marginTop="20dp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@id/ll_content"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:background="@drawable/ic_bottomsheet_bar"/>
    <LinearLayout
        android:id="@+id/ll_content"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_marginTop="15dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@id/iv_view"
        app:layout_constraintBottom_toBottomOf="parent"
        android:orientation="vertical">
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingHorizontal="20dp"
            android:orientation="vertical">
            <TextView
                android:id="@+id/tv_title_address"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginBottom="15dp"
                android:textColor="@color/black"
                android:layout_gravity="center_horizontal"
                android:text="주소 설정"
                style="@style/TvH4"/>
            <include
                android:id="@+id/l_address_search"
                layout="@layout/layout_address_search"/>
            <Button
                android:id="@+id/btn_address_map_search"
                android:layout_marginVertical="8dp"
                android:text="현재 위치로 주소 설정"
                android:textSize="14sp"
                android:fontFamily="@font/ibm_plex_sans_regular"
                android:gravity="center_vertical|left"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:paddingVertical="10dp"
                android:textColor="@color/gray500"
                app:strokeColor="@color/grayForBackground"
                app:rippleColor="@color/grayForBackground"
                app:icon="@drawable/ic_navigation"
                app:iconPadding="10dp"
                app:iconTint = "@color/gray500"
                app:iconSize = "25sp"
                style="@style/Widget.MaterialComponents.Button.OutlinedButton.Icon" />
            <View
                android:layout_width="match_parent"
                android:layout_height="2dp"
                android:background="@color/grayForBackground" />
        </LinearLayout>
        <include
            android:id="@+id/layout_address_empty_view"
            layout="@layout/layout_empty_view"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:visibility="visible"/>
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rv_address"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@color/grayForBackground"
            android:nestedScrollingEnabled="false"
            android:orientation="vertical"
            android:visibility="visible"
            app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
            tools:listitem="@layout/item_rv_address" />
    </LinearLayout>
</androidx.constraintlayout.widget.ConstraintLayout>
